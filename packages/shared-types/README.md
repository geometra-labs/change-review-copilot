# Shared Types

This package is reserved for shared TypeScript domain types once the web app and report schema start converging on stable contracts.

Keep it small. Add only types that are truly shared across app boundaries.
